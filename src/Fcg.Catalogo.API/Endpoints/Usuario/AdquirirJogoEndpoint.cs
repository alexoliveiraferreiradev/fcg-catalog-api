namespace Fcg.Catalogo.API.Endpoints.Usuario
{
    public static class AdquirirJogoEndpoint
    {


        //private static async Task<IResult> AdquirirJogo(
        //    [FromServices] ISender sender,
        //    [FromBody] AdquirirJogoCommand command,
        //    CancellationToken cancellationToken)
        //{
        //    var response = await sender.Send(command, cancellationToken);
        //    if (!response.Sucesso)
        //    {
        //        return Results.BadRequest(response.Mensagens);
        //    }
        //    return Results.Ok(response.Mensagens);
        //}
    }
}
